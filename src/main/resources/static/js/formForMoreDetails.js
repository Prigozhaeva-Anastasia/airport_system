window.onload=function() {
    let divForTransfers = document.getElementById('divForTransfers');

    divForTransfers.textContent = divForTransfers.innerText.substring(0, divForTransfers.innerText.length-2);
}